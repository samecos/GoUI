# 贡献指南

欢迎提交问题、文档修正和代码改进。先阅读 [README](README.md)，了解前端的运行方式、后端依赖和当前功能范围。

## 本地开发

使用 Node.js 22.12+ 或 24.x，在仓库根目录运行：

```sh
npm ci
npm run dev
```

默认连接当前网页主机的 `8090/ws`。需要其他后端地址时，参考 README 配置 `.env.local`。本仓库不包含后端、推理 Worker 或权重；单元测试和受控浏览器回归可以独立运行，真实对弈及分析需要兼容后端。

提交前运行：

```sh
npm test
npm run build
```

修改连接、会话、SGF 或回放行为时，请补充能体现行为差异的回归测试。修改界面时，请检查桌面和窄屏布局，并按 [README 的浏览器回归步骤](README.md#浏览器回归) 运行 `scripts/browser_smoke.py`。涉及真实服务行为的改动，在具备后端环境时运行 `scripts/browser_live.py`，并注明是否连接了推理 Worker；没有相应环境时在 PR 中说明未执行的验证。

保持修改与所解决的问题相关，遵循附近代码的现有风格。变更协议或功能范围时，同步更新文档；依赖变更应同时更新 `package.json`、`package-lock.json` 和相关第三方说明。

## 提交问题与 PR

问题报告请包含复现步骤、预期行为、实际行为，以及相关浏览器和 Node.js 版本。涉及连接或分析时，说明是否使用真实后端、是否有可用 Worker。截图或日志只需保留定位问题所需内容，并移除密钥、令牌、私人地址和其他敏感信息。

PR 描述简要说明：

- 解决什么问题，修改后行为如何。
- 已执行的测试与结果，以及未验证的环境或功能。
- 涉及界面时附修改前后截图；涉及配置或协议时说明兼容性影响。

不要提交 `.env` 等本地配置中的秘密、浏览器会话信息、日志、测试产物、构建目录、`node_modules/`、模型权重，或没有权利公开的第三方内容。可复用的配置示例应使用占位值，不含凭据。

## 贡献许可

向本项目提交并请求合入的原创贡献，按项目的 [Unlicense](LICENSE) 提供，允许任何人使用、复制、修改、商用、闭源和再分发，不要求署名或公开修改后的源码。贡献流程不要求额外签署 CLA。

请确保你有权按该协议提供贡献。引入第三方代码或资源时，保留其原有协议要求，注明来源并更新 [第三方说明](THIRD_PARTY_NOTICES.md)；不要将他人未授权的内容标为本项目原创代码。
