# 安全问题反馈

如果项目托管在 GitHub 且已启用私密漏洞报告，请在仓库的 **Security → Advisories → Report a vulnerability** 提交报告。此功能需要维护者在仓库设置中启用。

如果没有上述入口，也没有已公布的私密联系方式，可先创建不含漏洞细节的 Issue 请求联系渠道。不要在公开 Issue 中粘贴凭据、私人棋谱、会话标识或可直接利用的漏洞细节。

私密报告中可提供受影响的版本或提交、复现步骤、预期与实际行为、影响范围，以及去除敏感信息的日志。

## 部署相关

- `VITE_*` 配置会被打包到浏览器代码中，只能保存公开配置；不能用于保管密钥。
- 本项目是前端客户端，后端需要自行实现访问控制、请求校验和资源限制。浏览器中的按钮禁用与输入校验不构成服务端安全边界。
- 公网 HTTPS 页面需要可访问的 `wss://` 后端。服务部署者负责 TLS 和后端访问策略。
- 开发服务器和 `npm run preview` 用于开发与检查构建产物；生产环境使用静态文件服务。
