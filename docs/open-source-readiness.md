# 首次开源准备记录

检查日期：2026-09-08。此记录对应首次开源准备时的工作区，后续代码或依赖变更后应重新验证。

## 已完成

- 项目自有代码采用标准 [Unlicense](../LICENSE)，`package.json` 与锁文件使用相同的 SPDX 标识。
- [README](../README.md)、[贡献指南](../CONTRIBUTING.md) 和 [前端 WebSocket 契约](backend-protocol.md) 可在本仓库内阅读，不依赖开发者本机的后端文档路径。
- [第三方说明](../THIRD_PARTY_NOTICES.md) 区分项目授权、构建依赖及在线字体；生产构建自动附带项目许可证和第三方声明。
- `.gitignore` 排除本地环境配置、依赖、构建包、虚拟环境、日志与测试产物，保留 `.env.example`。
- 已配置 GitHub Actions 单元测试与构建（Node 22 / 24，Ubuntu / Windows）、Dependabot 依赖更新和 PR 模板。
- 已初始化本地 Git 仓库，尚无提交、远程仓库或公开发布。

## 实际验证

在 Windows、Node.js 24.14.1、npm 11.11.0 下，将按 Git 忽略规则筛出的项目文件复制到独立临时目录，从空依赖环境运行：

| 检查 | 结果 |
| --- | --- |
| `npm ci` | 成功；锁文件可独立安装 |
| `npm test` | 30 项通过，0 失败 |
| `npm run build` | 成功 |
| `npm run build -- --base=/yijian/ --outDir=dist-subpath` | 成功，HTML 静态资源路径包含 `/yijian/assets/` |
| 两种输出目录的授权文件 | 与源文件内容一致 |
| `npm audit --json`（项目目录） | 当次未报告已知依赖漏洞 |
| 文档文件链接与独立阅读复核 | 未发现缺失文件链接或阻碍按步骤运行的问题 |

本次未运行浏览器回归、真实后端联调或远程 GitHub Actions；不能据此声称 CI 的全部系统与 Node 版本已通过。

源码、测试、脚本、配置和文档的模式扫描未发现常见凭据或私人服务地址；npm 锁文件中的依赖来自公开 registry 并带完整性校验。依赖目录、虚拟环境、构建产物和测试报告按忽略规则排除，没有逐项审查；当前没有 Git 历史可检查。模式扫描不等于完整安全审计。

## 公开仓库创建时

- 确定托管平台与仓库名称，设置实际仓库地址后再提交、推送并公开。
- 如果使用 GitHub，启用私密漏洞报告，使 [安全反馈说明](../SECURITY.md) 中的私密入口可用，并检查首次 CI 结果。
- 如需让用户完整运行对弈与 AI 分析，提供兼容后端的公开获取方式。当前 README 已明确前端仓库不包含后端、Worker 或权重；前端可以单独开源。

现有 `dist.zip` 属于本地产物，已忽略，不作为本次开源源码包。发布静态站点时应使用重新构建后的输出目录。
